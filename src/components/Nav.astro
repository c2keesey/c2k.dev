---
const links = [
  { href: '/', label: 'Home' },
  { href: '/projects', label: 'Projects' },
  { href: '/about', label: 'About' },
];
---

<!-- Desktop top nav -->
<nav class="top-nav">
  <div class="nav-inner">
    <a href="/" class="logo">C2K</a>
    <div class="nav-links" id="nav-links">
      {links.map(link => (
        <a href={link.href} class="nav-link">
          {link.label}
        </a>
      ))}
      <div class="nav-indicator" id="nav-indicator"></div>
    </div>
  </div>
</nav>

<!-- Mobile bottom nav -->
<nav class="bottom-nav" id="bottom-nav">
  <div class="bottom-nav-bar" id="bottom-nav-pill">
    <div class="bottom-nav-highlight" id="bottom-nav-highlight"></div>
    <button class="bottom-tab" data-index="0" aria-label="Home">
      <svg class="tab-icon" width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.2">
        <path d="M2 6.5L8 2l6 4.5V13a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V6.5z"></path>
        <path d="M6 14V9h4v5"></path>
      </svg>
      <span class="tab-label">Home</span>
    </button>
    <button class="bottom-tab" data-index="1" aria-label="Projects">
      <svg class="tab-icon" width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.2">
        <rect x="2" y="2" width="5" height="5" rx="0.5"></rect>
        <rect x="9" y="2" width="5" height="5" rx="0.5"></rect>
        <rect x="2" y="9" width="5" height="5" rx="0.5"></rect>
        <rect x="9" y="9" width="5" height="5" rx="0.5"></rect>
      </svg>
      <span class="tab-label">Systems</span>
    </button>
    <button class="bottom-tab" data-index="2" aria-label="About">
      <svg class="tab-icon" width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.2">
        <rect x="3" y="2" width="10" height="12" rx="1"></rect>
        <path d="M6 5h4M6 7.5h4M6 10h2.5"></path>
      </svg>
      <span class="tab-label">About</span>
    </button>
  </div>
</nav>

<style>
  /* ── Desktop top nav ── */
  .top-nav {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(10, 10, 11, 0.8);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
  }

  .nav-inner {
    max-width: 720px;
    margin: 0 auto;
    padding: 0 1.5rem;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  @media (min-width: 1024px) {
    .nav-inner {
      max-width: 92%;
    }
  }

  .logo {
    font-family: var(--font-heading);
    font-size: 1.1rem;
    font-weight: 800;
    background: var(--gradient-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .nav-links {
    display: flex;
    gap: 0;
    position: relative;
  }

  .nav-link {
    padding: 0.4rem 0.75rem;
    font-size: 0.875rem;
    color: var(--text-muted);
    transition: color 0.2s;
    position: relative;
    z-index: 1;
    cursor: pointer;
  }

  .nav-link:hover {
    color: var(--text);
  }

  .nav-indicator {
    position: absolute;
    bottom: 0;
    left: 0;
    height: 100%;
    background: var(--bg-hover);
    border-radius: 6px;
    opacity: 0;
    pointer-events: none;
    z-index: 0;
  }

  /* Hide top nav links on mobile (keep logo) */
  @media (max-width: 767px) {
    .nav-links {
      display: none;
    }
  }

  /* ── Mobile bottom nav ── */
  .bottom-nav {
    display: none;
  }

  @media (max-width: 767px) {
    .bottom-nav {
      display: flex;
      justify-content: center;
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      z-index: 100;
      padding: 0 0 calc(20px + env(safe-area-inset-bottom, 0px));
      pointer-events: none;
    }

    .bottom-nav-bar {
      display: flex;
      align-items: center;
      position: relative;
      width: 220px;
      padding: 4px;
      background: rgba(14, 14, 16, 0.75);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border-radius: 10px;
      border: 1px solid rgba(168, 85, 247, 0.12);
      box-shadow: inset 0 1px 0 0 rgba(255, 255, 255, 0.03);
      pointer-events: auto;
    }

    .bottom-nav-highlight {
      position: absolute;
      top: 4px;
      height: calc(100% - 8px);
      border-radius: 7px;
      background: rgba(168, 85, 247, 0.10);
      border: 1px solid rgba(168, 85, 247, 0.15);
      pointer-events: none;
      transition: left 300ms cubic-bezier(.4, 0, .2, 1),
                  width 300ms cubic-bezier(.4, 0, .2, 1);
      will-change: left, width;
    }

    .bottom-tab {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      height: 38px;
      flex: 1 1 0;
      min-width: 0;
      padding: 0;
      border: none;
      background: transparent;
      color: var(--text-muted);
      cursor: pointer;
      border-radius: 7px;
      position: relative;
      z-index: 1;
      transition: color 250ms ease-out,
                  flex 300ms cubic-bezier(.4, 0, .2, 1);
      -webkit-tap-highlight-color: transparent;
    }

    .tab-icon {
      width: 16px;
      height: 16px;
      flex-shrink: 0;
    }

    .tab-label {
      font-family: var(--font-mono);
      font-size: 0.55rem;
      font-weight: 500;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      white-space: nowrap;
      overflow: hidden;
      width: 0;
      opacity: 0;
      transition: none;
    }

    /* Active state */
    .bottom-tab[data-active="true"] {
      color: var(--purple);
      flex: 2.2 1 0;
    }

    .bottom-tab[data-active="true"] .tab-label {
      width: auto;
      opacity: 1;
      transition: opacity 150ms ease-out 100ms;
    }
  }
</style>

<script>
  function initBottomNav() {
    const bottomNav = document.getElementById('bottom-nav');
    if (!bottomNav) return;

    const tabs = bottomNav.querySelectorAll<HTMLButtonElement>('.bottom-tab');
    const highlight = document.getElementById('bottom-nav-highlight') as HTMLElement;

    // Pure math — no DOM measurement needed
    // Bar: 220px, padding: 4px each side → inner: 212px
    // Flex: inactive=1, active=2.2, total=4.2
    const PADDING = 4;
    const INNER = 212;
    const FLEX_INACTIVE = 1;
    const FLEX_ACTIVE = 2.2;
    const TOTAL_FLEX = FLEX_INACTIVE * 2 + FLEX_ACTIVE; // 4.2
    const W_INACTIVE = INNER / TOTAL_FLEX * FLEX_INACTIVE; // 50px
    const W_ACTIVE = INNER / TOTAL_FLEX * FLEX_ACTIVE;     // 110px

    function moveHighlight(index: number) {
      if (!highlight) return;
      // Active tab position = padding + (inactive tabs before it) * W_INACTIVE
      const left = PADDING + index * W_INACTIVE;
      highlight.style.left = `${left}px`;
      highlight.style.width = `${W_ACTIVE}px`;
    }

    function setActiveTab(index: number) {
      tabs.forEach((tab, i) => {
        tab.setAttribute('data-active', String(i === index));
      });
      moveHighlight(index);
    }

    // Determine initial active tab from URL
    const pages = ['/', '/projects', '/about'];
    const path = location.pathname;
    const activeIdx = Math.max(0, pages.findIndex(p =>
      p === '/' ? path === '/' || path === '' : path.startsWith(p)
    ));

    // Set initial state without transitions
    if (highlight) highlight.style.transition = 'none';
    tabs.forEach((tab, i) => {
      tab.setAttribute('data-active', String(i === activeIdx));
      const label = tab.querySelector('.tab-label') as HTMLElement;
      if (label && i === activeIdx) {
        label.style.transition = 'none';
        label.style.opacity = '1';
      }
    });
    moveHighlight(activeIdx);
    requestAnimationFrame(() => {
      if (highlight) highlight.style.transition = '';
      tabs.forEach((tab) => {
        const label = tab.querySelector('.tab-label') as HTMLElement;
        if (label) { label.style.transition = ''; label.style.opacity = ''; }
      });
    });

    tabs.forEach((tab) => {
      tab.addEventListener('click', () => {
        const index = parseInt(tab.dataset.index || '0', 10);
        setActiveTab(index);
        window.dispatchEvent(new CustomEvent('nav:goto', { detail: { index } }));
      });
    });

    window.addEventListener('nav:update', ((e: CustomEvent) => {
      setActiveTab(e.detail.index);
    }) as EventListener);
  }

  initBottomNav();
  document.addEventListener('astro:after-swap', initBottomNav);
</script>
